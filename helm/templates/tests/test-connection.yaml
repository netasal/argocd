apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sentence-app.fullname" . }}-test-connection"
  labels:
    {{- include "sentence-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sentence-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
